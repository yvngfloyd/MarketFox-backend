import os
import logging
from typing import Any, Dict

from fastapi import FastAPI, Body
from fastapi.middleware.cors import CORSMiddleware
from openai import OpenAI
import uvicorn

# ----------------- Логгер -----------------

logger = logging.getLogger("marketfox")
logger.setLevel(logging.INFO)
handler = logging.StreamHandler()
handler.setFormatter(
    logging.Formatter("[%(asctime)s] [%(levelname)s] %(name)s: %(message)s")
)
logger.addHandler(handler)

# ----------------- FastAPI -----------------

app = FastAPI(
    title="MarketFox API (Groq, Railway)",
    description="Backend для ассистента по подбору товаров",
    version="0.5.0",
)

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_methods=["*"],
    allow_headers=["*"],
)

# ----------------- PROMPTS -----------------

# Промт для подбора товара — НОВЫЙ, без навязчивых уточнялок.
SYSTEM_PRODUCT_PICK = """
Ты — MarketFox, персональный ассистент по выбору товаров на маркетплейсах (Wildberries, Ozon и т.п.).

Твоя задача:
- по запросу пользователя подсказать, какие характеристики ему важнее всего;
- предложить несколько подходящих вариантов/типов товаров.

Формат ответа СТРОГО такой:

1. Очень короткое вступление 1–2 предложения, где ты показываешь, что понял запрос.
2. Далее нумерованный список рекомендаций по выбору, где каждый пункт — отдельный совет или пример типа товара.
3. Если уместно, можешь добавить 2–4 примера моделей в общем виде: "Razer Opus 7 — для тех, кто...", без ссылок и точных цен.

Требования к стилю и формату:
- Пиши только обычный текст, БЕЗ markdown-разметки: не используй *, **, -, заголовки, эмодзи или смайлики.
- Не используй заглушки "XXX Гц", "N", "Y". Если не знаешь точные цифры, просто опиши словами (например: "широкий частотный диапазон" вместо "20–20000 Гц").
- Не пиши ссылки, артикулы и точные цены. Можно писать диапазоны: "в районе 5–10 тыс. рублей".
- Отвечай всегда на русском языке.
- Ответ должен помещаться в 1–2 экрана Telegram, без огромных полотен текста.
- Если запрос слишком общий ("наушники", "молоток"), сначала задай 1–2 уточняющих вопроса и только потом давай рекомендации.
"""

# На будущее можно завести отдельные промты для подарков, сравнения и т.п.
SYSTEM_PROMPT_GENERIC = """
Ты дружелюбный консультант по товарам. Отвечай по-русски, понятно и по делу.
Дай пользователю практичные советы по его запросу.
"""

# ----------------- GROQ CLIENT -----------------


def get_groq_client() -> OpenAI:
    """
    Клиент для Groq через библиотеку openai.

    Нужна переменная окружения:
    GROQ_API_KEY=...
    """
    api_key = os.environ.get("GROQ_API_KEY")
    if not api_key:
        raise RuntimeError("GROQ_API_KEY is not set")

    # Groq работает через совместимый openai endpoint
    client = OpenAI(
        api_key=api_key,
        base_url="https://api.groq.com/openai/v1",
    )
    return client


async def call_groq(system_prompt: str, user_query: str) -> str:
    """Вызов Groq, возвращаем текст ответа."""
    client = get_groq_client()

    response = client.chat.completions.create(
        model="llama-3.1-8b-instant",
        messages=[
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": user_query},
        ],
        temperature=0.7,
        max_tokens=600,
    )

    return (response.choices[0].message.content or "").strip()


# ----------------- БИЗНЕС-ЛОГИКА -----------------


async def generate_reply(scenario: str, query: str) -> str:
    """
    Строим текст ответа для пользователя в зависимости от сценария.

    Сейчас по факту основное — product_pick.
    """
    try:
        if scenario == "product_pick":
            system_prompt = SYSTEM_PROMPT_PRODUCT
        else:
            system_prompt = SYSTEM_PROMPT_GENERIC

        text = await call_groq(system_prompt, query)
        if not text:
            raise ValueError("Empty reply from Groq")

        return text

    except Exception as e:
        # Логируем, но пользователю даём человеко-читабельный фолбэк
        logger.error("Groq API error: %s", e, exc_info=True)

        return (
            "Сейчас я не могу обратиться к нейросети, но вот базовый алгоритм выбора:\n\n"
            "1) Определи бюджет и 1–2 главные характеристики товара.\n"
            "2) Отсей варианты без отзывов и с очень низким рейтингом.\n"
            "3) Сравни 3–5 адекватных моделей по ключевым параметрам.\n"
            "4) Почитай негативные отзывы — они лучше всего показывают реальные минусы.\n\n"
            "Попробуй немного уточнить запрос, и я постараюсь помочь более предметно 🦊"
        )


# ----------------- ENDPOINT -----------------


@app.post("/marketfox")
async def marketfox_endpoint(payload: Dict[str, Any] = Body(...)) -> Dict[str, str]:
    """
    Основной вебхук для BotHelp.

    Из payload берём поле "Запрос" (что за товар ищет пользователь).
    scenario можно пробрасывать из BotHelp (например, 'product_pick', 'gift' и т.п.),
    но по умолчанию считаем, что сейчас идёт подбор товара.
    """
    logger.info("Incoming payload keys: %s", list(payload.keys()))

    # Что написал пользователь
    query = str(payload.get("Запрос") or "").strip()

    # Сценарий можно передавать из BotHelp через скрытое поле/метку
    scenario = (
        str(payload.get("scenario") or payload.get("Сценарий") or "product_pick").strip()
    )

    logger.info("User scenario=%s query=%s", scenario, query)

    if not query:
        # Ничего не написали — мягко просим сформулировать запрос
        reply_text = (
            "Похоже, вы пока не указали, что именно ищете. "
            "Напишите одним сообщением, какой товар вам нужен и примерный бюджет, "
            "например: «смартфон до 20 000» или «молоток до 3000 резиновый»."
        )
        return {"reply_text": reply_text, "scenario": scenario}

    reply_text = await generate_reply(scenario, query)

    return {
        "reply_text": reply_text,
        "scenario": scenario,
    }


# ----------------- HEALTHCHECK -----------------


@app.get("/")
async def root() -> Dict[str, str]:
    return {"status": "ok", "message": "MarketFox backend is running"}


# ----------------- LOCAL RUN -----------------

if __name__ == "__main__":
    uvicorn.run(
        "main:app",
        host="0.0.0.0",
        port=int(os.environ.get("PORT", 8000)),
        reload=False,
    )
